apiVersion: v1
kind: Service
metadata:
  name: pokeclone-backend
spec:
  selector:
    app: pokeclone-backend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
